group = ext.project_group
archivesBaseName = ext.project_name
description = 'A Java library for discovering the meta data of minecraft files'

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'com.google.code.gson', name: 'gson', version: '2.8.1'
}
